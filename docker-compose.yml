services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: eila
      POSTGRES_PASSWORD: eila123
      POSTGRES_DB: eila_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: