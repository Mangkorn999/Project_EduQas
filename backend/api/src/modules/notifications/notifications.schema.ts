import { z } from 'zod'
import { paginationSchema } from '../../utils/pagination'

export const listNotificationsQuerySchema = paginationSchema
export const deliveryStatusQuerySchema = paginationSchema
