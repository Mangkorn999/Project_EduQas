import type {Config} from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        prompt: ['Prompt', 'sans-serif'],
      },
    },
  },
};

export default config;
